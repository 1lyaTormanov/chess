import * as React from 'react'
import {FC} from "react";
import {FigureI} from "./gameTypes";


export const Figure: FC<FigureI> = (props) => {
    return(
        <div>
            
        </div>
    )
}