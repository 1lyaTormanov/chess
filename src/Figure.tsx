import * as React from 'react'
import {FC} from "react";
import {FigureI} from "./types";


export const Figure: FC<FigureI> = (props) => {
    return(
        <div>
            
        </div>
    )
}